# 开发任务：CPTSD 自我疗愈网站

你是开发这个网站的 coding agent。项目文档已经全部备好，**先读文档，再动手写代码**。

## 第 1 步：按顺序读这些文件

1. `docs/10_safety_guardrails.md` —— 安全护栏，最高优先级，先读
2. `docs/06_mvp_scope.md` —— 做什么 / 不做什么
3. `docs/07_implementation_notes.md` —— 数据模型 + 技术栈 + 实现顺序
4. `docs/08_screen_spec.md` —— 6 页页面规格 + 末尾验收脚本
5. `docs/09_ai_tool_routing.md` —— AI 辅助路由
6. `methods/methods.json` —— 29 个方法的结构化数据，直接 import
7. `CPTSD方法库.md` —— 方法原文（核对用）

## 第 2 步：产品是什么

CPTSD 自助疗愈网站，基于 Pete Walker《不原谅也没关系》。核心**不是聊天机器人**，而是把疗愈方法做成**确定性交互工具**（闪回急救、接地、内在批判者挑战），AI 只做两个辅助：识别 14 种批判者攻击、把自我批判改写得更温和。

**北极星**：情绪被过去劫持时，温柔地带人回到当下。

6 个页面：**Home / Rescue（闪回急救）/ Critic（内在批判者挑战）/ Grieve（哀悼）/ Learn（了解自己）/ Methods（方法库）**。

## 第 3 步：技术栈（已定，勿改）

Vite + React + TypeScript；React Context；localStorage；`methods/methods.json` 静态 import；Tailwind 或普通 CSS；React Router 或 hash。**不引后端、不做账号、不上 SSR。核心疗愈流程可离线。**

## 第 4 步：第一轮开发（只做这 5 块，做完停下，给用户验收）

1. 数据层（Entry / Method / FlashbackSession）
2. **安全层**：危机检测 `detectCrisis()` + 转介组件 + 接地组件（docs/10，程序确定性，AI 不参与）
3. **Home**（两个大按钮"我正闪回"+"内在批判者在攻击我" + 一键接地）
4. **Rescue**（闪回急救：13 步逐条 + 接地 + 强度前后记录）
5. **Critic**（内在批判者挑战：14 种攻击 → 思维纠正 → 用自己的话改写）

**先不要做 Grieve / Learn / Methods**。做完这 5 块就停下，等验收。

## 第 5 步：硬约束（必须遵守）

1. **危机检测**（自伤/自杀/当前危险）是程序确定性关键词匹配，命中即转介，AI 不参与。
2. **不做诊断、不做治疗、不给医疗建议**；不引导深挖创伤记忆。
3. **不做聊天机器人**：AI 只做两个辅助函数（docs/09），不自由对话。
4. 接地、"随时可停"、转介组件**从第一屏就内置**。
5. 文案**无羞耻、不否定感受、不强迫积极**；把闪回/羞耻/批判者正常化为"创伤后的适应反应"。
6. **大字号、低刺激、一步一件事**。

## 第 6 步：完成标准

跑通 `docs/08` 验收脚本的 **第 1–3 步 + 第 7 步安全验证**（输入"我不想活了"→ 任意页立即转介，停止流程）；每一步无 AI 也能走通；刷新后数据仍在（localStorage）。

## 最后

先完成第 1 步读文档，然后用几句话告诉我你理解的产品和第一轮实现计划，再开始写代码。遇到文档没写清楚的，按 README 第 8 节的"文档冲突优先级"处理（**安全 > 临床方法 > 范围 > 页面 > AI**），不要自己凭空发明。
