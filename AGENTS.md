# 项目开发说明（Codex 自动读取本文件）

本目录是「CPTSD 自我疗愈工具」——基于 Pete Walker《不原谅也没关系》的 CPTSD 自助疗愈网站。核心是**确定性交互工具**（闪回急救、接地、内在批判者挑战），AI 只做辅助，**不是聊天机器人**。

## 开发前必读（按顺序）
1. `docs/10_safety_guardrails.md`（安全护栏，最高优先级）
2. `docs/06_mvp_scope.md`
3. `docs/07_implementation_notes.md`（数据模型 + 技术栈）
4. `docs/08_screen_spec.md`（6 页规格 + 验收脚本）
5. `docs/09_ai_tool_routing.md`（AI 辅助路由）
6. `methods/methods.json`（29 个方法数据，直接 import）
7. `CPTSD方法库.md`（方法原文，核对用）

## 技术栈（已定，勿自行改选型）
Vite + React + TypeScript；状态 React Context；持久化 localStorage；方法数据 `methods/methods.json` 静态 import；样式 Tailwind 或普通 CSS；路由 React Router 或 hash。
**红线**：不引后端、不做账号、不上 SSR。核心疗愈流程可离线。

## 硬约束（安全优先，必须遵守）
- **危机检测**（自伤/自杀/当前危险）是**程序确定性**关键词匹配，命中即转介，AI 不参与。
- **不做诊断、不做治疗、不给医疗建议。**
- **不做聊天机器人**：AI 只做两个辅助（识别 14 种批判者攻击、把自我批判改写得更温和），见 docs/09。
- **不引导深挖创伤记忆**。
- 接地、"随时可停"、转介组件**从第一屏就内置**。
- 文案**无羞耻、不否定感受、不强迫积极**；把闪回/羞耻/批判者正常化为"创伤后的适应反应"。
- **大字号、低刺激、一步一件事**（闪回中用户"上头"，界面要少、字要大、节奏慢）。

## 开发顺序（第一轮只做以下 5 块，做完停下）
1. 数据层（Entry / Method / FlashbackSession）
2. **安全层**：危机检测 `detectCrisis()` + 转介组件 + 接地组件（docs/10，程序确定性）
3. **Home**（两个大按钮"我正闪回"+"内在批判者在攻击我" + 一键接地）
4. **Rescue**（闪回急救：13 步逐条 + 接地 + 强度前后记录）
5. **Critic**（内在批判者挑战：14 种攻击 → 思维纠正 → 改写）

**先不要做 Grieve / Learn / Methods**。第一轮只交这 5 块，给用户验收后再继续。

## 完成标准
跑通 `docs/08` 验收脚本的第 1–3 步 + 第 7 步安全验证（输入"我不想活了"→ 立即转介）；每一步无 AI 也能走通；刷新后数据仍在（localStorage）。

## 遇到文档冲突
安全 > 临床方法 > 范围 > 页面 > AI（见 README 第 8 节和 docs/10），不要凭空发明。
